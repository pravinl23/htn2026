# Historical workflow experiment

Atomic workflows and external execution were explored in an earlier direction. They are not part of the supported native Shabang app, and no current desktop product flow exposes them.

Some related server code may remain for tests or future evaluation. It must not be described as a live integration, and it needs a separate security/product review before revival.
