# Historical loop experiment

“Do it twice, Shabang does the rest” was an earlier product direction. It is not part of the supported native macOS app.

Some shared and server loop code remains in the repository for evaluation and historical context, but no current desktop UI exposes batch loop creation or execution. Do not describe it as a shipped Shabang capability. See [architecture](architecture.md) for the active scope.
