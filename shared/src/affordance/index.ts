// Shabang anywhere (docs/anywhere.md): affordances instead of labels, a kind of place instead of a flat page, priors
// per place and memory per role. Pure and unit-tested; the extension ranker and the native agent both import it.
export * from "./roles";
export * from "./pageKind";
export * from "./priors";
export * from "./memory";
