// Which key accepts a ghost, and how a place is learned (docs/accept-key.md). Pure decision layer: both clients
// import it, neither of them re-implements it.
export * from "./policy";
export * from "./observe";
